<?php

declare(strict_types=1);

if (! defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Cleanup hooks and data removal are added in later implementation phases.<?php
/**
 * Fired when the plugin is uninstalled.
 *
 * @package LegacyPopups
 */

declare(strict_types=1);

if (! defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Intentionally left minimal for the initial scaffold.
// Future versions can add optional cleanup routines here.